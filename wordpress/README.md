## Build Setup

```bash
# Build container serve at localhost
$ docker-compose up -d

# Stop container
$ docker-compose down -d

# Remove volume
$ docker-compose down --volumes

# Stop all containers
$ docker stop p $(sudo docker ps -aq)
```

```bash
Requires advanced-custom-fields-pro plugin
```